function handles = PlayMovie(handles)

movie(handles.axes1,handles.MaxProjectionMovie,2,6);  % movie(axis handle, movie, iteration, frames per second)
% movie(handles.axes1,handles.DeltaFOverFMovie,2,6);